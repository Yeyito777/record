#!/usr/bin/env bash
# create-worktree — create a record worktree.
#
# Usage:
#   create-worktree <name>

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "$(readlink -f "${BASH_SOURCE[0]}")")" && pwd)"
# shellcheck source=./worktree-common.sh
source "$SCRIPT_DIR/worktree-common.sh"

NAME="${1:-}"
[[ -n "$NAME" ]] || worktree_die "Usage: create-worktree <name>"
[[ "$NAME" != */* ]] || worktree_die "Pass a worktree name, not a path: $NAME"

WORKTREE_DIR="$RECORD_ROOT/.worktrees/$NAME"

[[ ! -e "$WORKTREE_DIR" ]] || worktree_die "Already exists: $WORKTREE_DIR"
if git -C "$RECORD_ROOT" show-ref --verify --quiet "refs/heads/$NAME"; then
  worktree_die "Branch already exists: $NAME"
fi

mkdir -p "$RECORD_ROOT/.worktrees"
git -C "$RECORD_ROOT" worktree add "$WORKTREE_DIR" -b "$NAME" HEAD

cat <<EOF

  ✓ Created worktree
  name:   $NAME
  path:   $WORKTREE_DIR
  branch: $NAME

EOF
